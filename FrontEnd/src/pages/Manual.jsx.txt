import React from 'react';
import './Manual.css';
import mapaImg from './assets/MAPA.jpg';
import logo from './assets/LOGO2.png';

function Manual() {
  const steps = [
    {
      id: 1,
      title: "Ensilla tu montura",
      desc: "Escoge tu caballo de acero de entre nuestra variada manada para cualquier sendero.",
      icon: "bx-cycling"
    },
    {
      id: 2,
      title: "Traza el rumbo",
      desc: "Ubica tu abrevadero (estación) más cercano en nuestro mapa del territorio.",
      icon: "bx-map" 
    },
    {
      id: 3,
      title: "¡Arre! A cabalgar",
      desc: "Confirma tu pago, desbloquea con un toque y lánzate a la aventura bajo el sol.",
      icon: "bx-purchase-tag-alt"
    }
  ];

  const handleOpenPDF = () => {
    window.open('/TERMINOS.pdf', '_blank');
  };

  return (
    <div className="manual-body anim-fade-in">
      <div className="manual-container paper-container anim-slide-up">
        
        <header className="manual-header">
          <div className="logo-wrapper">
            <img src={logo} alt="Logo HeyVaquero" className="manual-logo anim-float" />
          </div>
          <h1 className="rye-font old-west-title">Guía del Vaquero</h1>
          
          <div className="action-container">
            <button className="western-btn btn-start pulse-effect">
              <i className='bx bx-play-circle'></i> COMENZAR ORDENAR
            </button>
          </div>
        </header>

        <section className="steps-grid">
          {steps.map((step, index) => (
            <div 
              key={step.id} 
              className="step-card western-card anim-step" 
              style={{ animationDelay: `${(index + 1) * 0.2}s` }}
            >
              <div className="step-number leather-badge">{step.id}</div>
              <i className={`bx ${step.icon} step-icon icon-sway`}></i>
              <h3 className="card-title-vaquero">{step.title}</h3>
              <p>{step.desc}</p>
            </div>
          ))}
        </section>

        <section className="map-section anim-slide-up" style={{ animationDelay: '0.8s' }}>
          <h2 className="rye-font section-title-old">Puntos de Encuentro</h2>
          <div className="map-wrapper old-map-frame">
            <img 
              src={mapaImg} 
              alt="Mapa Monterrey" 
              className="map-img" 
            />
          </div>
        </section>

        <footer className="manual-footer anim-fade-in" style={{ animationDelay: '1s' }}>
          <button className="western-btn btn-terms" onClick={handleOpenPDF}>
            <i className='bx bxs-file-pdf'></i> LEER TÉRMINOS Y CONDICIONES
          </button>
        </footer>
        
      </div>
    </div>
  );
}

export default Manual;